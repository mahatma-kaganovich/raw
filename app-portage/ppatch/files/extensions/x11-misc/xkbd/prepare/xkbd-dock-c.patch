--- a/src/xkbd.c	2017-12-16 02:51:21.355040272 +0300
+++ b/src/xkbd.c	2017-12-16 03:46:52.737502848 +0300
@@ -183,6 +183,7 @@ int main(int argc, char **argv)
    char *font_name = NULL;
    int cmd_xft_selected = 0; /* ugly ! */
    int embed = 0;
+   int dock = 0;
    Bool use_normal_win = False;
    
    XEvent an_event;
@@ -226,6 +227,9 @@ int main(int argc, char **argv)
 	    case 'x' :
 	       embed = 1;
 	       break;
+	    case 'c' :
+	       dock = 1;
+	       break;
 	    case 'n' :
 	       use_normal_win = True;
 	       break;
@@ -370,6 +374,11 @@ int main(int argc, char **argv)
       wm_hints = XAllocWMHints();
       wm_hints->input = False;
       wm_hints->flags = InputHint;
+      if (dock) {
+	wm_hints->flags |= IconWindowHint | WindowGroupHint | StateHint;
+	wm_hints->initial_state = WithdrawnState;
+	wm_hints->icon_window = wm_hints->window_group = win;
+      }
       XSetWMHints(display, win, wm_hints );
 
       /* Tell the WM we dont want no borders */
