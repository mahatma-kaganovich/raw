# low RAM. no -pipe, etc
_FLAGS="$_FLAGS$CFLAGS_NATIVE$CFLAGS_M -Wa,--reduce-memory-overheads"
# ? -fconserve-stack

USE="speculative -ssp nossp nopie -sanitize -vtv -pie -pic"

CFLAGS="$CFLAGS $_FLAGS"
FFLAGS="$FFLAGS $_FLAGS"
FCFLAGS="$FCFLAGS $_FLAGS"
CXXFLAGS="$CXXFLAGS $_FLAGS"
#LDFLAGS="-Wl,-O1 -Wl,--as-needed -Wl,--hash-style=gnu -Wl,--sort-common"
LDFLAGS="$LDFLAGS -Wl,--no-keep-memory -Wl,--reduce-memory-overheads"
