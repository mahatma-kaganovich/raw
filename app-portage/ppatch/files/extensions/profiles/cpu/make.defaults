_FLAGS="$_FLAGS$CFLAGS_FAST$CFLAGS_CPU$CFLAGS_M$CFLAGS_SECURE"

CFLAGS="$CFLAGS $_FLAGS"
FFLAGS="$FFLAGS $_FLAGS"
FCFLAGS="$FCFLAGS $_FLAGS"
CXXFLAGS="$CXXFLAGS $_FLAGS $_XFLAGS"

# trick to workaround some -march=native detections
CPPFLAGS="-D__FAKE_FLAG=-march=native"
