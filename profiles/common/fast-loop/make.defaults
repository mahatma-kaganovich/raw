# mostly parent for unroll and graphite
# not broken in 5.2
_FLAGS="$_FLAGS -ftree-loop-distribution -ftree-loop-im -ftree-loop-ivcanon"
# todo: check relation on graphite and if no - move to "unroll"
_FLAGS="$_FLAGS -fvariable-expansion-in-unroller"
# testing
_FLAGS="$_FLAGS -fgcse-sm -fgcse-las"
#_FLAGS="$_FLAGS -fprefetch-loop-arrays"
