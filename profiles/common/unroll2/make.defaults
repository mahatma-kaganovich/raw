# looks like help other optimizers, but unsure & too increase code size & compile time
CFLAGS="$CFLAGS -ftracer"
# graphite
# mostly not broken in 5.3
CFLAGS="$CFLAGS -floop-block -floop-strip-mine -floop-interchange"
# experimental, broken code (fpo) in gmp with -fstrict-overflow (-O2)
#CFLAGS="$CFLAGS -floop-nest-optimize"
# broken: libvpx, grub, mupdf
#-floop-unroll-and-jam"

FFLAGS="$CFLAGS"
FCFLAGS="$CFLAGS"
CXXFLAGS="$CFLAGS"
CXXFLAGS="$CXXFLAGS -fvisibility-inlines-hidden"
