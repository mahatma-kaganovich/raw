#!/bin/bash
# starting from 1.19.0 default DISPLAY=:0.0 vs DISPLAY=:0,
# so this will force aggressive Xserver displays occupation
# and other voluntary things
export DISPLAY="${DISPLAY%.[0-9]}"
. ${0%-auto}
